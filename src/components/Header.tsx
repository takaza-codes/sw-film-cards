import React from 'react';
import CustomNav from "@/components/CustomNav";

const Header: React.FC = () => (
  <header className="bg-stone-50 shadow-md py-4">
    <CustomNav/>
  </header>
);

export default Header;
