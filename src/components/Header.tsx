import React from "react";
import CustomNav from "@/components/CustomNav";

const Header: React.FC = () => (
  <header className="bg-stone-100 shadow-md py-1">
    <CustomNav />
  </header>
);

export default Header;
